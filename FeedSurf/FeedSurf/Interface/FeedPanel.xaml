<UserControl x:Class="FeedSurf.Interface.FeedPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FeedSurf.Interface"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Vertical" Margin="10,10,10,10">
        <Label x:Name="FeedsTitleLabel" Content="🔗"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4*" />
                <ColumnDefinition Width=".7*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="UrlBox" Grid.Column="0"/>
            <Button x:Name="AddButton" Content="Add" Grid.Column="1"/>
            <Button x:Name="RemoveButton" Content="Remove" Grid.Column="2"/>
        </Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="FeedsPanel" Orientation="Vertical">
                
            </StackPanel>
        </ScrollViewer>
    </StackPanel>
</UserControl>
